{% extends 'base.html.twig' %}

{% block title %}Create product!{% endblock %}

{% block body %}
<div class="container" id="Product">
    <h3 class="card-title display-5 fw-bold">
        <img src="{{ asset('logo.png') }}" class="card-img-top" alt="Product" style="float: left; height: 75px; width: 75px">
    </h3>
    <div class="container mx-auto mt-4" id="Product">
        <div class="card-body" >
            <div class="row">
                <div class="col-md-12">
                    <div class="card-body card-text">
                        {{ form_start(form, {'attr': {'id': 'formProduct', 'onsubmit':'return validateForm();'}}) }}
                        {{ form_rest(form) }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    <button class="btn btn-success" name="Insert" type="submit">Save Product</button>
    <a href="/product/create" class="btn btn-secondary" style="float: right">Reset</a>
    <a href="/product" class="btn btn-secondary" style="float: right; margin-right: 2px">Homepage</a>
    </div>
</div>
    {{ form_end(form) }}
{% endblock %}

{% block footer_js %}
     <script src="{{ asset('js/create.js') }}"></script>
{% endblock %}
